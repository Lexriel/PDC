#include <stdio.h>

 static   char      a;     /* Ce programme ne fait
                pas grand chose */

void main()  {
      int n;
    char c;

                char zizette[] = "zizi\"zezette";
          int i ;
              for ( i = 0 ; i < 10 ;
                     i ++ ) { printf ("lol\n");}
          c = getchar;       /* on lit un caractère */ /* sur stdin */

if ( c== ' ' ) { n++;putchar(c);}
 else /* sinon,
         on ne fait rien */
   { ;}
}

#include <stdio.h>
/* Ce programme ne fait pas grand chose */ void main() {       int n;     char c;                char zizette[] = "zizi\"zezette";           c = getchar;       /* on lit un caractère */ /* sur stdin */ if ( c== ' ' ) { n++;putchar(c);} else /* sinon, on ne fait rien */  { ;}}
